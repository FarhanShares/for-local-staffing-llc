{
  "private": true,
  "name": "frontend2",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --port=3020",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.11"
  },
  "dependencies": {
    "frontend-module": "workspace:^1.0.0"
  }
}
